# 助盲跑安卓项目书

## 一、项目概述

“助盲跑”是一款面向视障跑者与陪跑志愿者的 Android 应用。项目通过账号体系区分“盲人用户”和“志愿者用户”，为盲人提供一键求助、预约陪跑、运动数据记录、志愿者评价等功能；为志愿者提供实时接单、查看预约、管理已接订单等功能。客户端采用 Kotlin + Jetpack Compose 开发，整体结构接近 MVVM：界面层负责展示与交互，ViewModel 维护页面状态，Repository 负责调用后端接口。

项目入口位于 `app/src/main/java/com/mio/applicationwork/MainActivity.kt`。应用启动后先初始化本地会话管理器 `SessionManager.init(this)`，再通过 `setContent` 加载 Compose 主题和导航图，具体代码在 `MainActivity.kt` 第 21-31 行。

## 二、技术架构

### 1. 前端技术

项目使用 Jetpack Compose 构建界面。所有页面都以 `@Composable` 函数形式实现，例如登录页 `LoginScreen` 定义在 `app/src/main/java/com/mio/applicationwork/ui/screen/login/LoginScreen.kt` 第 26-31 行，一键求助页 `QuickHelpScreen` 定义在 `app/src/main/java/com/mio/applicationwork/ui/screen/quickhelp/QuickHelpScreen.kt` 第 23-30 行。

### 2. 导航结构

页面路由统一定义在 `app/src/main/java/com/mio/applicationwork/ui/screen/Screen.kt`。其中 `Login`、`Register`、`Home`、`CreateReservation`、`ReservationList`、`RunData`、`RateVolunteer`、`QuickHelp` 等页面路由集中写在第 3-33 行。这样做的好处是页面跳转参数清晰，例如主页路由携带 `userType` 和 `userId`，便于不同身份进入不同功能。

真正执行页面跳转的是 `app/src/main/java/com/mio/applicationwork/ui/navigation/NavGraph.kt`。导航图以登录页作为起始页，代码在第 20-23 行；登录成功后跳转主页并清除登录页回退栈，代码在第 25-32 行；主页继续提供个人中心、创建预约、预约列表、运动数据、评价志愿者和一键求助等入口，代码在第 50-81 行。

### 3. 网络层

网络请求基于 Retrofit + OkHttp。接口声明集中在 `app/src/main/java/com/mio/applicationwork/data/api/ApiService.kt`，例如盲人登录接口 `/mangRen/login` 在第 19-21 行，志愿者登录接口 `/zhiYuan/login` 在第 59-61 行，实时匹配接口 `/piPei/piPei` 在第 99-101 行，运动数据接口 `/user/getRun` 和 `/user/addRun` 在第 111-117 行。

Retrofit 客户端配置在 `app/src/main/java/com/mio/applicationwork/data/api/RetrofitClient.kt`。后端基础地址 `BASE_URL` 写在第 17 行；登录后保存 token 的 `setToken` 方法在第 21-28 行；请求拦截器会在每次请求前自动添加 `Authorization: Bearer token`，代码在第 32-40 行；Retrofit 实例创建在第 59-67 行。

### 4. 数据模型

后端统一返回格式由 `ApiResponse<T>` 表示，定义在 `app/src/main/java/com/mio/applicationwork/data/model/Models.kt` 第 15-19 行。登录请求和登录返回数据定义在第 25-33 行；预约请求、预约列表项和接单请求定义在第 101-129 行；匹配请求、匹配对象和匹配响应定义在第 158-187 行；运动数据和添加运动数据请求定义在第 144-153 行、第 189-195 行。

## 三、关键功能及代码讲解

### 1. 登录、注册与 7 天免登录

登录状态由 `LoginUiState` 保存，位置是 `app/src/main/java/com/mio/applicationwork/ui/screen/login/LoginViewModel.kt` 第 16-24 行。它包含账号、密码、用户身份、是否勾选 7 天免登录、加载状态、错误信息和登录成功结果。

登录页面支持“盲人/志愿者”身份切换，界面代码在 `LoginScreen.kt` 第 60-76 行。两个身份分别对应 `UserRepository.USER_TYPE_MANGREN` 和 `UserRepository.USER_TYPE_ZHIYUAN`，后续登录时会根据该值调用不同接口。

点击登录后，`LoginViewModel.login()` 先检查账号和密码是否为空，代码在 `LoginViewModel.kt` 第 69-77 行；随后在协程中调用 `repository.login(...)`，代码在第 79-83 行。登录成功后，系统会继续拉取用户姓名，并根据是否勾选“7 天免登录”决定是否写入本地会话，核心代码在第 84-97 行。

真正调用后端登录接口的是 `app/src/main/java/com/mio/applicationwork/data/repository/UserRepository.kt` 的 `login` 方法，位置在第 12-46 行。该方法根据 `userType` 判断调用 `api.mangRenLogin(request)` 还是 `api.zhiYuanLogin(request)`，对应代码在第 21-25 行；登录成功后调用 `RetrofitClient.setToken(data.token)` 保存 token，代码在第 32-36 行。

7 天免登录由 `app/src/main/java/com/mio/applicationwork/data/local/SessionManager.kt` 实现。会话保存到 SharedPreferences 的逻辑在第 36-44 行；判断会话是否仍有效的逻辑在第 50-61 行，其中第 55-58 行会检查当前时间与登录时间差是否超过 7 天；退出登录时通过 `clearSession()` 清空本地数据并清除 Retrofit token，代码在第 72-76 行。

### 2. 角色化主页与功能入口

主页代码位于 `app/src/main/java/com/mio/applicationwork/ui/screen/home/HomeScreen.kt`。页面根据 `userType` 判断当前身份，代码在第 41 行；用户姓名优先从 `SessionManager` 中读取，代码在第 42-43 行。

主页顶部提供个人中心和退出登录入口，代码在第 45-55 行。点击退出时会调用 `SessionManager.clearSession()` 清除免登录信息，然后返回登录页，代码在第 51-52 行。

主页最重要的逻辑是按角色展示不同功能。盲人用户可以看到“一键求助、创建预约、查看预约、查看运动数据、评价志愿者”，代码在第 99-104 行；志愿者用户可以看到“一键接单、查看预约列表、查看运动数据”，代码在第 105-109 行。这种角色化入口保证了盲人端和志愿者端操作流程不同，但仍复用同一个主页组件。

### 3. 一键求助与实时匹配

一键求助功能是本项目的核心功能，代码主要在 `app/src/main/java/com/mio/applicationwork/ui/screen/quickhelp/QuickHelpViewModel.kt` 和 `QuickHelpScreen.kt`。

匹配页面的状态由 `MatchingUiState` 定义，位置在 `QuickHelpViewModel.kt` 第 20-36 行。它保存地点、用户身份、匹配阶段、匹配到的用户信息，以及本次跑步数据录入字段。

匹配阶段由 `MatchPhase` 枚举表示，定义在第 42 行，包含 `Idle`、`Matching`、`Matched`、`Error` 四种状态。界面层根据这四种状态切换不同 UI，代码在 `QuickHelpScreen.kt` 第 57-176 行：初始状态展示地点输入框和发起按钮，第 60-84 行；匹配中展示进度条和取消按钮，第 87-100 行；匹配成功展示对方信息和运动数据表单，第 103-165 行；错误状态展示重试按钮，第 168-175 行。

点击“发起求助”后调用 `QuickHelpViewModel.startMatching()`，位置在 `QuickHelpViewModel.kt` 第 70-108 行。该方法先检查地点是否为空，第 71-75 行；然后把页面状态改为 `Matching`，第 77-79 行；接着启动协程轮询后端匹配接口，第 80-107 行。轮询中每次调用 `matchRepo.piPei(...)`，如果 `response.data != null`，说明匹配成功，状态切换为 `Matched` 并保存对方用户信息，代码在第 83-94 行；如果暂时没有匹配成功，则等待 3 秒后继续请求，代码在第 95-106 行。

匹配接口封装在 `app/src/main/java/com/mio/applicationwork/data/repository/MatchRepository.kt`。发起匹配的 `piPei` 方法在第 25-47 行，它会构造 `PiPeiRequest(userType, userId, diDian)` 并调用 `api.piPei(request)`，代码在第 27-30 行；如果后端返回的内部 `data` 不为空，就代表匹配成功，判断逻辑在第 31-38 行。取消匹配的 `delPiPei` 方法在第 52-69 行。

用户在匹配中点击取消时，`QuickHelpViewModel.cancelMatching()` 会先取消本地轮询协程，再调用后端取消匹配接口，代码在 `QuickHelpViewModel.kt` 第 115-133 行。这样可以避免用户已经离开页面但仍留在后端匹配池中。

### 4. 匹配成功后的运动数据录入

匹配成功后，页面会显示本次跑步数据录入表单，位置在 `QuickHelpScreen.kt` 第 132-157 行。表单包含配速、时长、公里数字段，点击提交会调用 `viewModel.submitRunData()`，代码在第 149-153 行。

数据提交逻辑在 `QuickHelpViewModel.kt` 第 142-167 行。方法会把字符串输入转换为整数，第 143-146 行；若存在小于等于 0 的数据，则提示填写完整，代码在第 148-151 行；校验通过后调用 `userRepo.addRunData(...)` 提交到 `/user/addRun`，代码在第 153-166 行。

Repository 层对应方法是 `UserRepository.addRunData()`，位置在 `UserRepository.kt` 第 218-236 行。它构造 `AddRunRequest`，调用 `api.addRunData(request)`，并判断后端是否返回 `code == 200 && data == true`。

### 5. 预约陪跑

盲人用户可以提前创建预约，相关代码在 `app/src/main/java/com/mio/applicationwork/ui/screen/createreservation` 目录。

创建预约页面状态定义在 `CreateReservationViewModel.kt` 第 16-22 行，包含地点、预约时间、加载状态、错误信息和成功标记。用户选择日期和时间的界面逻辑在 `CreateReservationScreen.kt` 第 35-80 行：先通过 `DatePickerDialog` 选择日期，第 36-53 行；再通过 `TimePickerDialog` 选择时间，第 57-80 行；最终把日期和时间合并成毫秒时间戳保存。

提交预约时，`CreateReservationViewModel.submit()` 会先校验地点和时间是否填写，位置在 `CreateReservationViewModel.kt` 第 50-56 行；然后把毫秒时间格式化为后端需要的 `"yyyy-MM-dd HH:mm:ss"` 字符串，代码在第 58-60 行；最后调用 `repository.createYuYue(userId, state.diDian, timeStr)` 发起预约，代码在第 62-77 行。

Repository 层的创建预约方法在 `app/src/main/java/com/mio/applicationwork/data/repository/ReservationRepository.kt` 第 12-39 行。该方法构造 `CreateYuYueRequest` 并调用 `api.mangRenCreateYuYue(request)`，代码在第 16-19 行；如果返回的预约 ID 为负数，则认为创建失败，代码在第 22-30 行。

### 6. 预约列表、志愿者接单与取消

预约列表页面位于 `app/src/main/java/com/mio/applicationwork/ui/screen/reservationlist` 目录。页面状态定义在 `ReservationListViewModel.kt` 第 17-24 行，包含加载状态、错误信息、预约列表、操作加载状态、操作提示和志愿者端视图模式。

加载预约列表的核心方法是 `loadReservations()`，位置在 `ReservationListViewModel.kt` 第 45-70 行。它根据身份和视图模式调用不同接口：盲人用户调用 `repository.getMangRenYuYue(userId)`，代码在第 52-53 行；志愿者查看全部预约时调用 `repository.getAllYuYue()`，代码在第 54-55 行；志愿者查看自己的接单时调用 `repository.getZhiYuanYuYue(userId)`，代码在第 56-58 行。

界面层使用 `LazyColumn` 展示预约卡片，代码在 `ReservationListScreen.kt` 第 93-110 行。志愿者端顶部有“全部预约 / 我的接单”切换按钮，代码在第 58-75 行。

预约卡片的操作逻辑在 `ReservationListScreen.kt` 第 151-190 行：盲人端显示接单状态和取消预约按钮，第 151-164 行；志愿者端在未接单时显示“接单”按钮，第 173-177 行；在“我的接单”模式下显示取消接单按钮，第 178-182 行。

对应的 ViewModel 操作方法包括：盲人取消预约 `cancelMangRen()`，位置在 `ReservationListViewModel.kt` 第 73-89 行；志愿者接单 `acceptYuYue()`，位置在第 92-108 行；志愿者取消接单 `cancelZhiYuan()`，位置在第 111-127 行。Repository 层对应接口分别封装在 `ReservationRepository.kt` 第 98-114 行、第 116-133 行、第 135-151 行。

### 7. 运动数据查看

运动数据页面位于 `app/src/main/java/com/mio/applicationwork/ui/screen/rundata` 目录。`RunDataViewModel.loadData()` 负责根据用户身份和用户 ID 加载运动数据，代码在 `RunDataViewModel.kt` 第 25-41 行。它调用 `repository.getRunData(userType, userId)`，成功后把数据写入 `records`，代码在第 28-39 行。

Repository 层的 `getRunData()` 方法位于 `UserRepository.kt` 第 197-216 行。它构造 `RunDataRequest(userType, userId)`，调用 `api.getRunData(request)`，并在成功时返回运动记录列表，代码在第 200-207 行。

页面展示逻辑在 `RunDataScreen.kt`。首次进入页面时通过 `LaunchedEffect(Unit)` 自动加载数据，代码在第 23-25 行；顶部提供刷新按钮，代码在第 32-37 行；当有数据时，页面先展示运动总览卡片，包括总距离、总时长和记录数，代码在第 72-123 行；随后按列表展示每次跑步的距离、配速和时长，代码在第 125-159 行。

### 8. 志愿者评价

盲人用户可以对志愿者进行评分，页面位于 `app/src/main/java/com/mio/applicationwork/ui/screen/ratevolunteer` 目录。

评分状态定义在 `RateVolunteerViewModel.kt` 第 11-17 行，包含志愿者 ID、评分、加载状态、错误信息和提交成功标记。提交评分的 `submit()` 方法在第 34-59 行：先把输入的志愿者 ID 转为整数并校验，第 35-40 行；再调用 `repository.pingJiaZhiYuan(zhiYuanId, pingFen)` 提交评价，第 42-58 行。

界面层在 `RateVolunteerScreen.kt` 第 43-49 行输入志愿者 ID，在第 53-63 行通过 `Slider` 选择 1 到 5 分，在第 72-85 行点击按钮提交。Repository 层的 `pingJiaZhiYuan()` 位于 `ReservationRepository.kt` 第 153-170 行，内部构造 `PingJiaRequest` 并调用 `api.mangRenPingFen(request)`。

## 四、项目亮点

1. 双角色流程清晰：通过 `userType` 区分盲人和志愿者，登录、主页、预约、匹配等多个模块都能根据角色执行不同逻辑。

2. 实时匹配状态机明确：一键求助功能使用 `Idle / Matching / Matched / Error` 四阶段状态机，界面和业务状态同步清晰，便于维护和扩展。

3. 网络层封装统一：所有接口集中在 `ApiService.kt`，Repository 层统一将后端响应转换为 `Result`，减少界面层对网络细节的依赖。

4. 支持本地免登录：`SessionManager` 使用 SharedPreferences 保存 token、用户 ID、用户身份和登录时间，实现 7 天内自动登录。

5. 功能闭环完整：项目从登录注册、发起求助、匹配志愿者、记录运动数据，到预约、接单、评价，形成了助盲跑业务的完整闭环。

## 五、后续可优化方向

1. 接入真实定位：当前匹配主要依赖用户手动输入地点，后续可接入 Android 定位权限和地图 SDK，实现当前位置自动获取和距离计算。

2. 增加语音辅助：面向视障用户，后续可加入语音播报、语音输入和无障碍 TalkBack 适配，提高实际可用性。

3. 优化匹配机制：当前客户端通过 3 秒轮询实现实时匹配，后续可改为 WebSocket 或服务端推送，降低延迟和流量消耗。

4. 加强安全性：当前 Retrofit 日志会输出较多请求信息，正式发布前应降低日志级别，并避免输出完整 token。

5. 增加测试：可以为 Repository、ViewModel 和关键状态机补充单元测试，保证登录、预约、匹配、提交运动数据等流程稳定。
